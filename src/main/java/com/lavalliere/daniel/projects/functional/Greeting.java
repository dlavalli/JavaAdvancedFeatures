package com.lavalliere.daniel.projects.functional;

@FunctionalInterface
public interface Greeting {
    void printMessage();  // All interface methods are abstract so no need to add abstract to the method
}
